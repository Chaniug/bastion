<?xml version="1.0" encoding="utf-8"?>
<!-- 图标背景：完全透明。盾牌 + 金色锁孔前景直接浮在桌面壁纸 / SplashScreen
     整体背景上，不绘制任何底色 —— 实现"透明背景"图标。

     关键点（避免再次出现 Honor 等 OEM 上的"黑色圆环"）：
     Android 12+ 的 SplashScreen 会把 adaptive-icon 裁进一个圆形"舞台"，
     舞台背景色由主题属性 android:windowSplashScreenIconBackgroundColor 决定。
     只要该属性未被显式给出透明值，部分 OEM 会回退默认深色 → 黑环。
     因此本仓库在 values(-night)-v31/colors.xml 的 Theme.Bastion 中已把
     windowSplashScreenIconBackgroundColor 显式设为 @android:color/transparent，
     让图标圆区直接透出 splash 整体背景，而非兜底深色。 -->
<color xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@android:color/transparent" />
