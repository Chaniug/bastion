name: Android CI debug

on:
  push:
    branches: [ main, dev ]
    paths:
      - 'Bastion/**/*.kt'
      - 'Bastion/**/*.java'
      - 'Bastion/**/*.xml'
      - 'Bastion/**/*.gradle'
      - 'Bastion/**/*.gradle.kts'
      - 'Bastion/**/*.kts'
      - 'Bastion/gradle/**'
      - 'Bastion/gradlew'
      - 'Bastion/gradlew.bat'
      - '.github/workflows/main.yml'
  pull_request:
    branches: [ main, dev ]
    paths:
      - 'Bastion/**/*.kt'
      - 'Bastion/**/*.java'
      - 'Bastion/**/*.xml'
      - 'Bastion/**/*.gradle'
      - 'Bastion/**/*.gradle.kts'
      - 'Bastion/**/*.kts'
      - 'Bastion/gradle/**'
      - 'Bastion/gradlew'
      - 'Bastion/gradlew.bat'
      - '.github/workflows/main.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: android-ci-debug-${{ github.ref }}
  cancel-in-progress: true

jobs:
  android-ci:
    name: Lint, test diagnostics, and debug build
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: "Bastion"

    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-read-only: ${{ github.event_name == 'pull_request' }}

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run lint
        run: ./gradlew :app:lintDebug --stacktrace

      - name: Run unit tests (diagnostic)
        continue-on-error: true
        run: ./gradlew :app:testDebugUnitTest --stacktrace

      - name: Upload unit test reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: debug-unit-test-reports
          path: |
            Bastion/app/build/reports/tests/testDebugUnitTest
            Bastion/app/build/test-results/testDebugUnitTest
          retention-days: 7
          if-no-files-found: ignore

      - name: Build Debug APK
        run: ./gradlew :app:assembleDebug --stacktrace

      - name: List debug APK outputs
        run: find app/build/outputs/apk/debug -maxdepth 1 -type f -print || true

      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: debug-apk
          path: |
            Bastion/app/build/outputs/apk/debug/*.apk
            Bastion/app/build/outputs/apk/debug/*.APK
          retention-days: 7
          if-no-files-found: error
