# 省电与内存优化计划（2026-09-06 实测）

> 接力的 AI：本文所有结论均基于 **2026-09-06 01:09~01:20 在荣耀 BKQ-AN00 / API 37 真机实测**
> （版本 `1.0.0-dev-be36a81`，debug 构建）。每条结论都附了判定依据与复现命令，**先复测再动手**。

---

## 📌 执行进度（2026-09-06 01:28 更新）

| 序 | 事项 | 状态 |
|---|---|---|
| 1 | 修 `canAuthenticate` | ✅ **已推送 `47dddc2`**（3 处，见 3.3）；守卫测试本地通过；**待 CI + 真机复测** |
| 2 | 删 `BitwardenViewModel` 诊断埋点 | ⏳ 未开始 |
| 3 | `scudo` 长时采样（20 分钟） | ⏳ 未开始 |
| 4 | release 包内存复测 | ⏳ 未开始 |

**醒后第一步（按顺序）**：

```bash
# ① 查 CI 是否绿，并挖真实单测数字（CI 单测 non-blocking，绿 ≠ 测试过）
gh run list -R Chaniug/bastion --branch dev --limit 2
gh run view <run-id> -R Chaniug/bastion --log | grep -aoE "total=[0-9]+ failed=[0-9]+.*verdict=[A-Z]+"

# ② 下 debug-apk 装包，然后跑 15 秒采样——期望从 1218 降到接近 0
adb logcat -c && sleep 15 && adb logcat -d | grep -c "hasEnrolledFingerprints.*com.bastion.app"
```

复测通过后，再做第 2 项（删埋点），两项可合并为一次提交。

---

## 一、结论摘要

| 项 | 定性 | 证据 |
|---|---|---|
| `canAuthenticate` 每秒 81 次 | 🔴 **确定 bug，待修** | 15 秒采样 1218 次；根因是 2 处 Composable 漏写 `remember` |
| `[anon]` 133 MB | 🟢 **不是泄漏** | 冷启动 8 秒即固定、静置后一字不差；单段 virtual 2.45 GB / PSS 133 MB = 地址空间预留 |
| `scudo` 22→68 MB | 🟡 **待观察** | native malloc 增长 46 MB，需判断收敛性 |
| Activity / 视图泄漏 | 🟢 **无** | `Activities=1`、`ViewRootImpl=1`、`Views=11`、`WebViews=0` |
| `BitwardenViewModel` 双实例 | ✅ **已修**（`be36a81`） | 冷启动 `created` 由 2 次 → 1 次 |
| ART 指令超限 | ✅ **已收官**（复核） | `instruction limit` 0 条 |

---

## 二、内存：已查清的部分

### 2.1 快照（运行约 6 分钟，pid 15100）

```
TOTAL PSS  288 MB   RSS 468 MB   单进程（无多进程翻倍）
  Private Other       147 MB  ← 最大项
  Native Heap          52 MB
  System               31 MB
  Graphics             31 MB   (EGL 27.8 MB，正常)
  Java Heap            20 MB
  Bitmap      132 个 / 2 MB   ✅ 正常
  SQL                 1.9 MB
  Activities=1  ViewRootImpl=1  Views=11  ✅ 无泄漏
  Proxy Binders=89（偏高，与高频 Binder 同线，修完 canAuthenticate 后复测）
```

### 2.2 `[anon]` 133 MB 的定性过程（**方法比结论重要**）

**第一步：增长曲线实验**（单点快照说明不了任何问题）

```bash
snap() { P=$(adb shell pidof com.bastion.app | tr -d '\r'); \
  adb shell run-as com.bastion.app showmap "$P" | \
  awk '$NF=="[anon]"{a+=$3} /scudo/{s+=$3} END{printf "anon=%dKB scudo=%dKB", a+0, s+0}'; }
```

| 时点 | anon | scudo |
|---|---|---|
| 冷启动后 8 秒（T0） | **135952 KB** | 22360 KB |
| 静置到 53 秒（T1） | **135952 KB** | 22360 KB |
| 运行 6 分钟后 | **135952 KB** | 68163 KB |

⇒ `[anon]` **一字不差**，启动时一次性预分配，**非增长性泄漏**。

**第二步：段结构**（区分"单个大块"与"碎片累积"）

`[anon]` **只有 1 段**：`virtual=2,574,496 KB（2.45 GB）/ PSS=135,952 KB`。
虚拟地址巨大、实际提交仅 133 MB ⇒ 典型的**堆地址空间预留 + 按需提交**。

**第三步：排除假设**

- ❌ **Argon2 / KDF**：查库确认 `local_keepass_databases` 与 `keepass_remote_sources` **均 0 行**，本机根本没在用 KDBX；`bitwarden_vaults` 用 PBKDF2 600k 迭代（非内存密集）。
- ❌ **LeakCanary 等调试库**：`app/build.gradle:325-329` 处于注释状态（2.14 不支持 API 37，无稳定版 3.0）。debug 仅 `minifyEnabled false` + `ui.tooling`。
- ❌ **多进程翻倍**：`ps -A` 确认只有 1 个 `com.bastion.app` 进程。

### 2.3 唯一待观察项：`scudo`（native malloc）

22 MB（T0）→ 68 MB（6 分钟）增长约 46 MB。可能是正常的图片/网络缓存，也可能是小泄漏。
**定性方法**：长时运行多点采样，看是否**收敛**（趋平）而非线性增长。

```bash
# 每 60 秒采一次，采 20 次（20 分钟）
for i in $(seq 1 20); do echo "t=${i}min $(snap)"; sleep 60; done
```

判据：**趋平 = 缓存，线性增长 = 泄漏**。

---

## 三、耗电：确定项（P0，建议优先修）

### 3.1 现象

15 秒采样内 `com.bastion.app` 发起 **1218 次** `hasEnrolledFingerprints` ≈ **81 次/秒**。
每次都是跨进程 Binder 调用 BiometricService，且**非操作时段也持续**。

> 注：不同时段测得的频率不同（早前测到 10 次/秒，本次 81 次/秒），
> 因频率取决于**重组频率**（滚动/动画/TOTP 倒计时越高越频繁），故取等级而非定值。

### 3.2 根因：两处 Composable 漏写 `remember`

```kotlin
// ui/password/PasswordListContent.kt:622   ← 每次重组都执行
val canUseBiometric = activity != null && appSettings.biometricEnabled
                      && biometricHelper.isBiometricAvailable()   // 跨进程 Binder

// ui/screens/NoteListScreen.kt:217   ← 同款写法
val canUseBiometric = activity != null && settings.biometricEnabled
                      && biometricHelper.isBiometricAvailable()
```

`biometricHelper` 本身用了 `remember { }`（621 / 216 行），但**查询结果没缓存** —— 每次重组都重新查。

**这不是设计选择，是漏写**，项目里同一判断已有三处写对：

| 位置 | 写法 |
|---|---|
| `MasterPasswordLockingSettingsScreen.kt:56` | `remember(biometricHelper) { ... }` ✅ |
| `PasskeyListScreen.kt:328` | `remember(appSettings.biometricEnabled) { ... }` ✅ |
| `AuthComponents.kt:61` | `remember { ... }` ✅ |

### 3.3 修法（✅ 已完成，`47dddc2`）

**实际改了 3 处**（比初判多一处）：

| 文件 | 原行 | 说明 |
|---|---|---|
| `PasswordListContent.kt` | 622 | 密码页，随滚动/动画重组 |
| `NoteListScreen.kt` | 217 | 笔记页（同结构，`settings.biometricEnabled`） |
| **`TotpListContent.kt`** | **412** | ⚠️ **初判时 `grep \| head -30` 被截断而漏掉**。该页 `sharedTickSeconds`（413 行 `produceState`）**每秒**刷新 ⇒ 每秒打一次指纹服务，比密码页更稳定持久 |

```kotlin
// 三处统一写法（NoteListScreen 用 settings.biometricEnabled）
val canUseBiometric = remember(activity, appSettings.biometricEnabled) {
    activity != null && appSettings.biometricEnabled && biometricHelper.isBiometricAvailable()
}
```

> **教训**：`grep ... | head -N` 的截断会漏掉命中、导致误判。**找齐调用点应去掉 head 全量列出再下结论**
> —— 本次因此差点漏掉验证器页这个更严重的点。

**逐个读代码确认后未改动**（低频，非主因）：

| 位置 | 为什么不用改 |
|---|---|
| `DeleteConfirmDialog.kt:39-41` | **已在 `remember(biometricEnabled)` 内**（初判据 grep 单行输出误判，读代码后排除） |
| `SettingsScreen.kt:299` | 已在 `remember(biometricHelper)` 内 |
| `PasskeyListScreen.kt:328` | 已在 `remember(...)` 内 |
| `SettingsScreen.kt:959` | 在 `onClick` 回调内，仅点击时执行 |
| `PasswordDetailScreen.kt:340` | 在 `startVerificationForDeletion()` 函数体内，仅调用时执行 |
| `AddEditNoteScreen:1077` / `CardWalletScreen:1550` / `SettingsScreen:1073` / `PasswordDetailScreen:1140` | 均在 `if (showXXXDialog)` 块内，仅弹窗时求值 |

### 3.4 验收

1. 本地：`:app:compileDebugKotlin` 绿；
2. **必跑守卫测试**：改了 `PasswordListContent`，`MultiPasswordSaveRegressionGuardTest` 有源码文本断言；
   `./gradlew :app:testDebugUnitTest --tests "com.bastion.app.viewmodel.MultiPasswordSaveRegressionGuardTest" --tests "com.bastion.app.ui.TotpSwipeSelectionRegressionGuardTest"`
3. CI：dev 绿（**CI 单测 non-blocking，要挖 `total=.*failed=` 真实数字**）；
4. 真机：装包后 15 秒采样，
   `adb logcat -d | grep -c "hasEnrolledFingerprints.*com.bastion.app"`，
   期望**从 1218 降到接近 0**（仅剩设置变更时的偶发调用）。

---

## 四、收尾项（P1）

### 4.1 删除 `BitwardenViewModel` 诊断埋点

`be36a81` 已真机验证通过（`BitwardenViewModel created` 只出现 1 次），埋点可以退役。

删除范围（`BitwardenViewModel.kt`）：

- `199-213` 行：诊断注释段
- `203-209` 行：`Log.i("BASTION-PROFILE", "BitwardenViewModel created instance=...")` 及创建堆栈
- `284-290` 行：`Log.i("BASTION-PROFILE", "loadVaults costMs=...")`

> `loadVaults` 里 `238` 行附近的**「墙钟耗时 ≠ 主线程阻塞」注释要保留** —— 那是防误判的长期知识，不是诊断代码。

### 4.2 `activityViewModel()` 的 KDoc 需更新

`ui/SharedViewModels.kt` 里写了"等 `SimpleMainScreen` 拆分完成后废弃"。
但拆分已从待办出列（见 5.1），故该 helper **实际成为终态**，KDoc 应按终态表述。

---

## 五、已出列项（不要再做）

### 5.1 `SimpleMainScreen` 拆分 —— **出列**

- **性能理由已消失**：JIT 指令拆分（批 1~5）2026-09-05 20:04 实测收官，
  `PasswordListContent` 与 `SimpleMainScreen` 双双降至 16384 以下，0 警告。
  见 `docs/passwordlistcontent-拆分计划-2026-09.md` 第九节。
- **别再拿文件行数下定论**：ART 判定单位是方法指令数，且 **Kotlin 局部函数编译为独立合成方法、
  不计入主函数指令数** —— `SimpleMainScreen` 内 1311 行的 `RenderMainSurface` 是局部函数，拆它零收益。
  按文件行数它只排全项目第 11（占 main 源集 1.2%）。
- 唯一权威信号是 logcat 的 `Method exceeds compiler instruction limit`。

### 5.2 `loadVaults` 双实例 —— **已完成**（`be36a81`）

---

## 六、执行顺序建议

| 序 | 事项 | 理由 |
|---|---|---|
| 1 | 修 `canAuthenticate`（2 处 + 1 处顺带） | 确定的耗电 bug，改动小、收益直接 |
| 2 | 删 `BitwardenViewModel` 诊断埋点 | 验证已通过，埋点是临时代码 |
| 3 | `scudo` 长时采样（20 分钟） | 只有拿到收敛曲线才能定性，否则别动 |
| 4 | release 包内存复测 | debug 无混淆，需确认 release 基线 |
| ~~5~~ | ~~SimpleMainScreen 拆分~~ | **出列** |

---

## 七、复用手法

- **判断泄漏靠曲线不靠快照**：`冷启动 T0 / 静置 T1 / 长跑后` 三点对比，单点数字毫无意义。
- **逐段映射**：`adb shell run-as <pkg> showmap <pid>`（需 run-as，debug 包可用），
  配合 `awk '$NF=="[anon]"'` 按类型聚合；`virtual ≫ PSS` 且单段 ⇒ 地址空间预留，非泄漏。
- **频率类问题先找调用点再看频率**：`grep` 找齐所有调用点，对比写法差异（有无 `remember`），
  比直接读频率数字更快定位。
